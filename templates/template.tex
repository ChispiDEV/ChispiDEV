\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[left=1in, right=1in, top=1in, bottom=1in]{geometry}
\usepackage{xcolor} % Para colores personalizados
\usepackage{fontspec} % Para fuentes personalizadas
\usepackage{enumitem} % Para listas personalizadas
\usepackage{titlesec} % Para personalizar los títulos
\usepackage{hyperref} % Para hipervínculos
\usepackage{setspace} % Para controlar el interlineado

% Configuración de colores y fuentes
\definecolor{mypurple}{HTML}{3c1642}
\definecolor{mydarkblue}{HTML}{086375}
\definecolor{myblue}{HTML}{1dd3b0}
\definecolor{mygreen}{HTML}{132a13}
\definecolor{mylightgreen}{HTML}{31572c}
\setmainfont{TeX Gyre Heros}

% Configuración de títulos
\titleformat{\section}{\color{mygreen}\large\bfseries}{}{0em}{}[\titlerule]
\titleformat{\subsection}{\color{mylightgreen}\normalsize\bfseries}{}{0em}{}

% Configuración de hipervínculos
\hypersetup{
    colorlinks=true,
    urlcolor=mydarkblue,
    linkcolor=myblue
}


\begin{document}

% Inserción automática de contenido desde Markdown por Pandoc
$for(header-includes)$
$header-includes$
$endfor$

$if(title)$
\begin{center}
    {\Huge \textbf{\textcolor{mypurple}{$title$}}} \\[1mm]
$endif$

$if(location)$
    $location$ \\[1mm]
$endif$

$if(email)$
    \href{mailto:$email$}{$email$} \\[1mm]
$endif$

$if(links)$
    $for(links)$
        \href{$it.url$}{$it.label$} \\[1mm]
    $endfor$
$endif$
\end{center}

% Contenido principal
$body$

\end{document}
